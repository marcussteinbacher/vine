To run a script copy/move it into the parent folder, e.g. `/ma` where config.py is located!

# cd into the project folder
cd ma

# copy all scripts into the parent folder
cp -r scripts/. .

# run a script
python run_volatiliy_forecasts.py --help